//Dependencies

const express = require('express');

//Instantiate the server

const app = express();












//Listen method

app.listen(3001, () => {
    console.log(`API server now on port 3001!`);
});